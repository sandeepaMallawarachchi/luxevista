<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:icon="@mipmap/logo"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Luxevista"
        tools:targetApi="31">
        <activity android:name=".AttractionDetailActivity" />
        <activity android:name=".ServiceDetailActivity" />
        <activity android:name=".RoomDetailActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".AdminRoomActivity" />
        <activity android:name=".AdminServiceActivity" />
        <activity android:name=".AdminAttractionActivity" />
        <activity android:name=".AdminBookingActivity" />
        <activity android:name=".AddRoomActivity" />
        <activity android:name=".AddServiceActivity" />
        <activity android:name=".AddAttractionActivity" />
        
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>